# `notebooks`

Папка для исследовательских ноутбуков.

Используется для ручного анализа данных, экспериментов с embeddings, проверок качества поиска и одноразовых исследований. Production-логика должна переноситься из ноутбуков в `src/` или `scripts/`.
