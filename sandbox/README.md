# `sandbox`

Изолированная зона для временных экспериментов.

Код и данные здесь не считаются частью стабильного пайплайна. Полезно для быстрых проверок идей, которые еще не готовы попасть в `src/` или `scripts/`.
